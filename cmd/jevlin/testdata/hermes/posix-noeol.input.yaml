model: gpt

# >>> jevlin agents install >>>
# the configuration above ended without a final newline; uninstall restores that
hooks:
  pre_tool_call:
    - command: '/home/u/.tokendrop/bin/jevlin hook -config /home/u/.tokendrop/tokendrop.toml hermes pre_tool_call'
      matcher: "terminal"
# <<< jevlin agents install <<<
